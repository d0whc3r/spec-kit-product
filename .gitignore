# Build artifacts
dist/
# Examples copied into the published site (single source of truth: /examples).
# Synced by `pnpm examples:sync` locally and by the pages workflow on deploy.
web/examples/
.wiki-staging/
upstream-catalog-issue-*.md
/.specify/
/specs/
.agents/skills/speckit-product-*
.claude/skills/speckit-product-*
.github/agents/speckit.product.*.agent.md
.github/prompts/speckit.product.*.prompt.md

# Editor and OS metadata
.DS_Store
.vscode/
.idea/
*.swp
*.tmp

# Dependency caches (none expected, but defensive)
node_modules/

dist/
